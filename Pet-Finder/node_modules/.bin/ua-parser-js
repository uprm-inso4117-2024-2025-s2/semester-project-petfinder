#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../recompose/node_modules/ua-parser-js/script/cli.js" "$@"
  ret=$?
else 
  node  "$basedir/../recompose/node_modules/ua-parser-js/script/cli.js" "$@"
  ret=$?
fi
exit $ret
